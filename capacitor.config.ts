import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'app-start-blank-ionic',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
